# CPython-android runtime dependencies
# These are installed on the device via pip

# HTTP/WebSocket server
aiohttp>=3.9.0

# User script dependencies (pure Python, pip install on device)
requests>=2.31.0
websockets>=12.0
pyyaml>=6.0
fuzzywuzzy>=0.18.0
openpyxl>=3.1.0
pymysql>=1.1.0
